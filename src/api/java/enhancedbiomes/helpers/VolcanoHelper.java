package enhancedbiomes.helpers;

import java.io.File;

import net.minecraft.server.MinecraftServer;
import net.minecraftforge.common.config.Configuration;

public class VolcanoHelper 
{
	/*public int[] getVolcanoPosition(int volcanoNo) {
		int[] result = new int[3];
		File configFile = new File("saves/" + MinecraftServer.getServer().getFolderName() + "/Volcano Positions.ebf");
		Configuration config = new Configuration(configFile);
		config.load();
		config.save();
		return null;
	}
	
	public void addVolcanoPosition() {
		File configFile = new File("saves/" + MinecraftServer.getServer().getFolderName() + "/Volcano Positions.ebf");
		Configuration config = new Configuration(configFile);
		config.load();
		config.save();
		configFile.
	}*/
}
